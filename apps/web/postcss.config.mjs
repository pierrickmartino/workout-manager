// Tailwind CSS v4 is a PostCSS plugin. No tailwind.config.js is needed — the
// theme lives in app/globals.css via the CSS-first `@theme` block.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
